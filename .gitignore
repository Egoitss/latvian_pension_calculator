# Personal config — NEVER commit
local_config.py

# Runtime download counter (persisted state, not source)
download_count.json
download_count.lock

# Runtime AI daily-budget counters (persisted state, not source)
ai_budget.json
ai_budget.lock
rec_budget.json
rec_budget.lock

# Security scan reports (generated, not source)
.security/

# Claude Code project instructions — personal dev tool
CLAUDE.md
.env
.env.local

# Python artefacts
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp

# Superpowers scratch (brainstorm servers, ledgers)
.superpowers/
